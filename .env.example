# ADHD Budget Assistant - Environment Variables
# Copy this to .env and fill in your values

# ===========================================
# Database Configuration
# ===========================================
# PostgreSQL connection settings
DB_HOST=db                          # Container hostname (db for Docker)
DB_PORT=5432                        # Default PostgreSQL port
DB_NAME=adhd_budget                 # Database name
DB_USER=budget_user                 # Database user
DB_PASSWORD=                        # REQUIRED: Set a strong password (no default provided)

# ===========================================
# Enable Banking API Configuration
# ===========================================
# Required for bank account integration
# Register at https://enablebanking.com to get credentials
ENABLE_APP_ID=                      # Your application ID (used as 'kid' in JWT)
ENABLE_PRIVATE_KEY_PATH=            # Path to private key file for RS256 signing
ENABLE_CERT_PATH=                   # Path to certificate file uploaded to Enable Banking
ENABLE_API_URL=https://api.enablebanking.com  # Production API URL
# For sandbox testing, use: https://api.sandbox.enablebanking.com

# ===========================================
# MCP Server Configuration
# ===========================================
# Model Context Protocol server for AI agent integration
MCP_TOKEN=                          # REQUIRED: Bearer token for MCP authentication
MCP_AUTH_TOKEN=                     # REQUIRED: Internal auth token
MCP_PORT=8081                       # MCP server port

# ===========================================
# API Server Configuration
# ===========================================
API_TOKEN=                          # REQUIRED: Bearer token for API authentication
API_PORT=8082                       # API server port

# ===========================================
# WhatsApp Business Cloud API (Optional)
# ===========================================
# Required only if using WhatsApp notifications
# Leave empty if not using WhatsApp integration
WHATSAPP_PHONE_ID=                  # WhatsApp Business phone number ID
WHATSAPP_TOKEN=                     # WhatsApp Business API token
WHATSAPP_WEBHOOK_SECRET=            # Webhook verification secret

# ===========================================
# Redis Configuration (Optional)
# ===========================================
# Used for caching and job queuing
REDIS_HOST=redis                    # Container hostname
REDIS_PORT=6379                     # Default Redis port

# ===========================================
# Proxy/TLS Configuration
# ===========================================
# For production deployment with HTTPS
DOMAIN=localhost                    # Your domain name (e.g., budget.example.com)
ACME_EMAIL=                         # Email for Let's Encrypt certificate notifications

# ===========================================
# Notes
# ===========================================
# 1. For local development, minimal required variables are:
#    - DB_PASSWORD (change from default)
#    - MCP_TOKEN (if using MCP features)
#
# 2. For production deployment, additionally configure:
#    - Enable Banking credentials (all ENABLE_* variables)
#    - DOMAIN and ACME_EMAIL for HTTPS
#    - Strong, unique tokens for all *_TOKEN variables
#
# 3. For sandbox testing with Enable Banking:
#    - Use sandbox URL: https://api.sandbox.enablebanking.com
#    - Follow sandbox setup at: https://enablebanking.com/docs/api/sandbox/
#
# 4. Security best practices:
#    - Never commit .env file to version control
#    - Use strong, randomly generated tokens/passwords
#    - Rotate credentials regularly
#    - Use Docker secrets or cloud secret management in production